<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.wmti.mapper.SurveyResultMapper">

    <!-- 설문 결과 저장 -->
    <insert id="saveSurveyResult" parameterType="org.scoula.wmti.entity.SurveyResult">
        INSERT INTO wmti_profile (member_id, wmti_code, answers_json, created_at)
        VALUES (#{memberId}, #{wmtiCode}, #{answersJson}, #{submittedAt})
    </insert>

    <!-- 사용자 ID로 설문 결과 조회 -->
    <select id="findByMemberId" resultType="org.scoula.wmti.entity.SurveyResult">
        SELECT wmti_id, member_id, wmti_code, answers_json, created_at
        FROM wmti_profile
        WHERE member_id = #{memberId}
    </select>
</mapper>

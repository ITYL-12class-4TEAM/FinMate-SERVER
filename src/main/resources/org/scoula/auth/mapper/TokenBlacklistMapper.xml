<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.scoula.auth.mapper.TokenBlacklistMapper">

    <insert id="insertBlacklistToken">
        INSERT INTO token_blacklist (token, token_type, reason, revoked_at, expires_at)
        VALUES (#{token}, #{tokenType}, #{reason}, NOW(), #{expiresAt})
    </insert>

    <select id="isTokenBlacklisted" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM token_blacklist
        WHERE token = #{token}
          AND expires_at > NOW()
    </select>

</mapper>
